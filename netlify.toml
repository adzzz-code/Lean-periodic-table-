# Déploiement Netlify (alternative à GitHub Pages).
# Site servi à la racine : BASE_PATH reste '/' (valeur par défaut).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
