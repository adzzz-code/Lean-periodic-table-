// @ts-check
import { defineConfig } from 'astro/config';

// Site statique. `base` ajustable selon l'hébergement (ex. GitHub Pages).
export default defineConfig({
  site: 'https://lean-atelier.example',
});
